# Frontend constraints

- Patients and clinicians use the same product version.
- Never display hidden chain-of-thought; only `public_summary` events.
- Lesion overlays render only validated server regions.
- Preserve cancel, resume, reconnect and explicit unavailable states.
- Keep basic keyboard focus, reduced-motion and mobile support.
